"TF2TeamRadar"
{
    // Core Settings
	
    "update_interval"    "0.1"		// How often the radar updates, in seconds
    "radar_size"         "2560.0"	// The in-game units the radar covers (Larger value = zoom out)
    "radar_scale"        "0.225"	// The size of the radar on the screen (0-1)
	"show_disguised_spies"    "1"	// Whether to show disguised enemy spies

    // Default colors (RGBA Format)
	
    "color_self"         "255 255 0 255" 	// The player's own icon color
    "color_teammate_healthy" "0 255 0 255" 	// Healthy teammates icon
    "color_teammate_low" "255 0 0 255"		// Low health teammates icon color
    "color_ping"         "255 255 0 255" 	// Ping markers icon color

    // Pings
	
    "max_pings"          "5"		// Maximum pings to show at a time
    "ping_duration"      "5.0"		// Duration of each ping in seconds
    "ping_cooldown"      "3.0"		// Cooldown between pings in seconds
}
